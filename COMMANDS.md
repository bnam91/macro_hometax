# 명령어 가이드

> 💡 **마크다운 미리보기:** `Cmd+Shift+V` (Mac) / `Ctrl+Shift+V` (Windows)

---

## 📋 명령어 목록

1. `npm start` / `npm run taxgo` - 세금계산서 발급 (전체 프로세스)
2. `npm run taxgo:dev` - 세금계산서 발급 (개발자 모드)
3. `npm run hometax` - 홈택스 로그인만
4. `npm run hometax:dev` - 홈택스 로그인만 (개발자 모드)
5. `npm run start:profile` - 세금계산서 발급 (별칭)
6. `npm run start:secret` - 기타 용도

---

## 🧾 세금계산서 발급 (전체 프로세스)

### `npm start` / `npm run taxgo`

**기능:** 홈택스 세금계산서 발급 전체 자동화
- 로그인 → Google Sheets 데이터 읽기 → 건별발급 화면 → 공급받는자/품목 입력 → 발급

```bash
npm start
# 또는
npm run taxgo
```

---

## 🔐 홈택스 로그인만

### `npm run hometax`

**기능:** 홈택스 로그인까지만 수행 (세금계산서 발급 없음)
- 로그인 → 인증서 선택 → 비밀번호 입력 → 메뉴 진입 완료

```bash
npm run hometax
```

**언제 사용?** 로그인만 테스트하거나 인증서 선택 확인할 때

---

## 🔧 개발자 모드

### `npm run taxgo:dev` / `npm run hometax:dev`

**기능:** 일반 모드와 동일하지만 **상세 디버깅 정보 출력**
- 드라이브/인증서 목록, 프레임 URL, 상태 정보 등

```bash
npm run taxgo:dev    # 전체 프로세스 (개발자 모드)
npm run hometax:dev   # 로그인만 (개발자 모드)
```

**언제 사용?** 문제 발생 시 원인 파악용

---

## 📊 명령어 비교

| 명령어 | 로그인 | 세금계산서 발급 | 디버깅 |
|--------|--------|----------------|--------|
| `npm start` | ✅ | ✅ | ❌ |
| `npm run taxgo:dev` | ✅ | ✅ | ✅ |
| `npm run hometax` | ✅ | ❌ | ❌ |
| `npm run hometax:dev` | ✅ | ❌ | ✅ |

---

## 💡 추천 사용법

```bash
# 처음 사용: 로그인만 테스트
npm run hometax

# 문제 발생: 개발자 모드로 확인
npm run taxgo:dev

# 정상 사용: 일반 모드
npm start
```
